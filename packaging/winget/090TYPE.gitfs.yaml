# Трёхфайловая форма — та, которую принимает microsoft/winget-pkgs.
# Однофайловая (singleton) не распознаёт PortableCommandAlias, и команда
# получила бы имя файла: пользователь набирал бы gitfs-win-x64.
#
#   winget validate --manifest packaging\winget
PackageIdentifier: 090TYPE.gitfs
PackageVersion: 0.1.0
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
